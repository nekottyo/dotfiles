#run-shell "powerline-daemon -q"
# Prefix
set-option -g prefix C-z
# status
set -g status-interval 10

set-option -g buffer-limit 32
set-option -g history-limit 20000
set-option -g default-command ""

# KeyBindings
# pane
unbind 1
bind 1 break-pane
bind 2 split-window -v
bind 3 split-window -h

bind C-r source-file ~/.tmux.conf
bind C-p kill-window
bind C-0 kill-window
unbind &
bind -r ^[ copy-mode
bind -r ^] paste-buffer

unbind y
bind y choose-buffer

set -s escape-time 0

bind C-c  source-file ~/.tmux/new-session_with_ams
bind c  source-file ~/.tmux/new-session
bind 0 kill-pane

# ウィンドウのインデックスを1から始める
set -g base-index 1

# ペインのインデックスを1から始める
setw -g pane-base-index 1

# | でペインを縦に分割する
# - でペインを横に分割する
bind | split-window -h
bind - split-window -v

# Vimのキーバインドでペインを移動する
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind -r C-h select-window -t :-
bind -r C-l select-window -t :+

# Vimのキーバインドでペインをリサイズする
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# マウス操作を有効にする
#setw -g mode-mouse on
#set -g mouse-select-pane on
#set -g mouse-resize-pane on
#set -g mouse-select-window on
set-option -g mouse on
bind -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'copy-mode -e'"

# 256色端末を使用する
#set -g default-terminal "xterm-256color"
#set-option -g default-terminal "xterm-256colo"

# true color
set -g default-terminal "xterm-256color"
set-option -ga terminal-overrides ",xterm-256color:Tc"


#copy系の情報
set-window-option -g mode-keys vi
bind -Tcopy-mode-vi 'v' send -X begin-selection
bind -Tcopy-mode-vi 'y' send -X copy-selection


# shell
set-option -g default-shell /bin/zsh
set-option -g default-command /bin/zsh

## クリップボード共有を有効にする
#set-option -g default-command "reattach-to-user-namespace -l zsh"


#run-shell "powerline-daemon -q"
#source "~/Library/Python/2.7/lib/python/site-packages/powerline/bindings/tmux/powerline.conf"
#source ~/.vim/bundle/powerline/powerline/bindings/tmux/powerline.conf


#source ~/.vim/bundle/powerline/powerline/bindings/tmux/powerline_tmux_1.9_plus.conf
#### COLOUR (Solarized dark)

### status line を更新する間隔を 1 秒にする
#set-option -g status-interval 1
#
## window-status を中央揃えで配置する
#set-option -g status-justify "centre"
#
## status line の背景色を指定する。
#set-option -g status-bg "colour238"
#
## status line の文字色を指定する。
#set-option -g status-fg "colour255"
#
## status-left の最大の長さを指定する。
#set-option -g status-left-length 60
#
## status-left のフォーマットを指定する。
#set-option -g status-left "#[fg=colour255,bg=colour241]Session #S #[default]"
#
## status-right の最大の長さを指定する。
#set-option -g status-right-length 80
#
## status-right のフォーマットを指定する。
#set-option -g status-right "#[fg=colour255,bg=colour241]#(tmux-mem-cpu-load --colors --interval 1) #[fg=colour255,bg=colour241] #h | %m/%d %H:%M:%S#[default]"
##
## window-status のフォーマットを指定する。
#set-window-option -g window-status-format " #I: #W "
##
## カレントウィンドウの window-status のフォーマットを指定する

## copy to clipboard
### for Linux
if-shell "which xsel" '\
  bind-key -T copy-mode-vi y send -X copy-pipe-and-cancel "xsel -ib"; \
  bind-key -T copy-mode-vi enter send -X copy-pipe-and-cancel "xsel -ib"; \
'
### for Cygwin
if-shell "which putclip" '\
  bind-key -T copy-mode-vi y send -X copy-pipe-and-cancel "iconv -s -t SJIS | putclip"; \
  bind-key -T copy-mode-vi enter send -X copy-pipe-and-cancel "iconv -s -t SJIS | putclip"; \
'
# prefix + r で設定ファイルを再読み込み
unbind r
bind   r source-file ~/.tmux.conf

# 複数window 同期設定
bind e set-window-option synchronize-panes
#bind E setw synchronize-panes off


# syncronize-panesがonならステータスバーのカレントの色を変える
#set-option -gw window-status-current-format "#(tmux show-window-options synchronize-panes | grep -q 'on$' && echo '#[fg=white,bg=red]')#F#I:#W"


#### COLOUR (Solarized 256)

# default statusbar colors
set-option -g status-bg colour235 #base02
set-option -g status-fg colour136 #yellow
set-option -g status-attr default

# default window title colors
set-window-option -g window-status-fg colour244 #base0
set-window-option -g window-status-bg default
#set-window-option -g window-status-attr dim

# active window title colors
set-window-option -g window-status-current-fg colour166 #orange
set-window-option -g window-status-current-bg default
#set-window-option -g window-status-current-attr bright

# pane border
set-option -g pane-border-fg colour235 #base02
set-option -g pane-active-border-fg colour245 #base01

# message text
set-option -g message-bg colour235 #base02
set-option -g message-fg colour166 #orange

# pane number display
set-option -g display-panes-active-colour colour33 #blue
set-option -g display-panes-colour colour166 #orange

# clock
set-window-option -g clock-mode-colour colour64 #green

# bell
set-window-option -g window-status-bell-style fg=colour235,bg=colour160 #base02, red

## statusline
set -g status-justify "centre"
set -g status "on"
set -g status-attr "none"
set -g message-command-bg "colour215"
set -g status-left-length "100"
set -g pane-active-border-fg "colour180"
set -g status-bg "colour58"
set -g message-command-fg "colour235"
set -g pane-border-fg "colour215"
set -g message-bg "colour215"
set -g status-left-attr "none"
set -g status-right-attr "none"
set -g status-right-length "100"
set -g message-fg "colour235"
setw -g window-status-fg "colour180"
setw -g window-status-attr "none"
setw -g window-status-activity-bg "colour58"
setw -g window-status-activity-attr "underscore"
setw -g window-status-activity-fg "colour180"
setw -g window-status-separator ""
setw -g window-status-bg "colour58"
set -g status-left "#[fg=colour58,bg=colour180] #S #[fg=colour180,bg=colour215,nobold,nounderscore,noitalics]#[fg=colour235,bg=colour215] #W #[fg=colour215,bg=colour58,nobold,nounderscore,noitalics]#[fg=colour180,bg=colour58] #H #[fg=colour58,bg=colour58,nobold,nounderscore,noitalics]"
set -g status-right "#[fg=colour58,bg=colour58,nobold,nounderscore,noitalics]#[fg=colour180,bg=colour58] %a #[fg=colour215,bg=colour58,nobold,nounderscore,noitalics]#[fg=colour235,bg=colour215] #W %R #[fg=colour180,bg=colour215,nobold,nounderscore,noitalics]##[fg=colour58,bg=colour180]#(tmux-mem-cpu-load --interval 2)"
setw -g window-status-format "#[fg=colour58,bg=colour58,nobold,nounderscore,noitalics]#[default] #I #W #[fg=colour58,bg=colour58,nobold,nounderscore,noitalics]"
setw -g window-status-current-format "#[fg=colour58,bg=colour215,nobold,nounderscore,noitalics]#[fg=colour235,bg=colour215] #I #W #[fg=colour215,bg=colour58,nobold,nounderscore,noitalics]"
