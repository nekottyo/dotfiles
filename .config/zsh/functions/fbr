# vim: ft=zsh
# fbr - checkout git branch
local branches branch
branches=$(git branch -vv) &&
branch=$(echo "$branches" | fzf +m) &&
# カレント (*) と worktree 使用中 (+) のマーカー行はブランチ名が 2 列目に来る
git checkout $(echo "$branch" | awk '{if ($1 == "*" || $1 == "+") print $2; else print $1}')
